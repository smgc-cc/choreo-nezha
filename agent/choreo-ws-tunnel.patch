diff --git a/cmd/agent/connection_config.go b/cmd/agent/connection_config.go
--- a/cmd/agent/connection_config.go
+++ b/cmd/agent/connection_config.go
@@ -1,12 +1,17 @@
 package main

 import (
+	"context"
 	"crypto/tls"
+	"net"
+	"strings"

+	"github.com/coder/websocket"
 	"github.com/nezhahq/agent/model"
 	"google.golang.org/grpc"
 	"google.golang.org/grpc/credentials"
 	"google.golang.org/grpc/credentials/insecure"
 )

 type connectionConfigTuple struct {
 	Server      string
@@ -28,6 +33,28 @@ func loadConnectionConfigTuple() connectionConfigTuple {
 	}
 }

+func (c connectionConfigTuple) newClient() (*grpc.ClientConn, error) {
+	if strings.HasPrefix(c.Server, "ws://") || strings.HasPrefix(c.Server, "wss://") {
+		return grpc.NewClient(
+			"passthrough:///nezha-ws-tunnel",
+			grpc.WithTransportCredentials(insecure.NewCredentials()),
+			grpc.WithContextDialer(func(ctx context.Context, _ string) (net.Conn, error) {
+				wsConn, _, err := websocket.Dial(ctx, c.Server, &websocket.DialOptions{
+					CompressionMode: websocket.CompressionDisabled,
+				})
+				if err != nil {
+					return nil, err
+				}
+				return websocket.NetConn(context.Background(), wsConn, websocket.MessageBinary), nil
+			}),
+			grpc.WithPerRPCCredentials(c.Auth),
+		)
+	}
+	return grpc.NewClient(c.Server, c.dialOptions()...)
+}
+
 func (c connectionConfigTuple) dialOptions() []grpc.DialOption {
 	var securityOption grpc.DialOption
 	if c.TLS {
diff --git a/cmd/agent/main.go b/cmd/agent/main.go
--- a/cmd/agent/main.go
+++ b/cmd/agent/main.go
@@ -265,7 +265,7 @@ func run() {

 	for {
 		connectionConfig := loadConnectionConfigTuple()
-		conn, err = grpc.NewClient(connectionConfig.Server, connectionConfig.dialOptions()...)
+		conn, err = connectionConfig.newClient()
 		if err != nil {
 			printf("与面板建立连接失败: %v", err)
 			retry()
@@ -607,6 +607,21 @@ func doSelfUpdate(config updateConfigTuple, useLocalVersion bool) (exit bool) {

 	printf("检查更新: %v", v)
 	var latest *selfupdate.Release
+	updateRepo := strings.TrimSpace(config.updateRepo)
+	if updateRepo == "" {
+		updateRepo = "smgc-cc/choreo-nezha"
+	}
 	switch {
+	case updateRepo != "":
+		updater, erru := selfupdate.NewUpdater(selfupdate.Config{
+			BinaryName: binaryName,
+		})
+		if erru != nil {
+			printf("更新失败: %v", erru)
+			return
+		}
+		latest, err = updater.UpdateSelf(v, updateRepo)
 	case config.useGiteeToUpgrade:
 		updater, erru := selfupdate.NewGiteeUpdater(selfupdate.Config{
 			BinaryName: binaryName,
diff --git a/cmd/agent/runtime_config_consumers.go b/cmd/agent/runtime_config_consumers.go
--- a/cmd/agent/runtime_config_consumers.go
+++ b/cmd/agent/runtime_config_consumers.go
@@ -37,6 +37,7 @@ type taskFeatureGates struct {
 type updateConfigTuple struct {
 	useAtomGitToUpgrade bool
 	useGiteeToUpgrade   bool
+	updateRepo          string
 }

 type startupConfigView struct {
@@ -85,6 +86,7 @@ func updateConfigTupleFrom(config *model.AgentConfig) updateConfigTuple {
 	return updateConfigTuple{
 		useAtomGitToUpgrade: config.UseAtomGitToUpgrade,
 		useGiteeToUpgrade:   config.UseGiteeToUpgrade,
+		updateRepo:          config.UpdateRepo,
 	}
 }

diff --git a/model/config.go b/model/config.go
--- a/model/config.go
+++ b/model/config.go
@@ -41,6 +41,7 @@ type AgentConfig struct {
 	UseIPv6CountryCode          bool            `koanf:"use_ipv6_country_code" json:"use_ipv6_country_code"`     // 默认优先展示IPv6旗帜
 	UseGiteeToUpgrade           bool            `koanf:"use_gitee_to_upgrade" json:"use_gitee_to_upgrade"`       // 强制从Gitee获取更新
 	UseAtomGitToUpgrade         bool            `koanf:"use_atomgit_to_upgrade" json:"use_atomgit_to_upgrade"`   // 强制从AtomGit获取更新
+	UpdateRepo                  string          `koanf:"update_repo" json:"update_repo,omitempty"`               // 自定义 GitHub 更新仓库
 	DisableNat                  bool            `koanf:"disable_nat" json:"disable_nat"`                         // 关闭内网穿透
 	DisableSendQuery            bool            `koanf:"disable_send_query" json:"disable_send_query"`           // 关闭发送TCP/ICMP/HTTP请求
 	IPReportPeriod              uint32          `koanf:"ip_report_period" json:"ip_report_period"`               // IP上报周期
